import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Colores corporativos Touchliga
  static const primary = Color(0xFF1F2841); // Navy exacto de marca
  static const primaryLight = Color(0xFF1B2C4F);
  static const secondary = Color(0xFFFF6D00); // Naranja exacto de marca (Touchliga)
  static const secondaryDark = Color(0xFFC44F00);

  // Fondo
  static const background = Color(0xFFF4F6F8);
  static const surface = Colors.white;

  // Texto
  static const textPrimary = Color(0xFF1C1C1C);
  static const textSecondary = Color(0xFF6B7280);
  static const onPrimary = Colors.white;
  static const onSecondary = Color(0xFF1C1C1C);

  // Estados
  static const success = Color(0xFF2E7D32);
  static const warning = Color(0xFFF57C00);
  static const error = Color(0xFFD32F2F);

  // Bordes
  static const border = Color(0xFFE0E0E0);
}
